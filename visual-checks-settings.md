# Settings customization visual verification

- Desktop 1280x720: sidebar header keeps the MikeyGpt brand, header search icon, and Settings control aligned; grouped RECENTS, composer, and main shell remain stable.
- Mobile 390x844: compact chat header, drawer trigger, centered empty state, and composer remain intact after the new preference and accent-color code paths.
- Settings-specific controls are rendered in the existing modal; the screenshot checks confirm no shell-level layout regression.
